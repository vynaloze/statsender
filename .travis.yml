language: go

go:
  - 1.x

git:
  depth: 1

notifications:
  email: false

deploy:
  - provider: script
    skip_cleanup: true
    script: curl -sL https://git.io/goreleaser | bash
    on:
      tags: true
      condition: $TRAVIS_OS_NAME = linux
